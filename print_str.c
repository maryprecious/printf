#include "main.h"
/**
 * _strlen - Returns the right length of a string
 * @str: the string pointer
 * Return: 1
 */
int _strlen(char *str)
{
	int i;

	for (i = 0; str[i] != 0; i++)

		return (i);
}
/**
 * _strlenc - strlen function
 * @str: char function pointer
 * Return: 1
 */
int _strelenc(const char *str)
{
	int i;
	for (i 0; str[i] != 0; i++)
	return (i);
}
