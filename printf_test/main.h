#ifndef MAIN_H
#define MAIN_H

#include <stdarg.h>
#include <unistd.h>
#include <stdlib.h>

int _putint(int in);
int putchr(char c);
int _printf(const char *format, ...);
int dee_puts(char *st);
#endif
